---
---

After downloading the `.crt`, `.key`, and `.jwt` files to your working directory, rename them using the following filenames:

- `nginx-repo.crt`
- `nginx-repo.key`
- `license.jwt`

During installation, the script will copy these files to the required system locations.
